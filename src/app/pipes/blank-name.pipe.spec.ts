import { BlankNamePipe } from './blank-name.pipe';

describe('BlankNamePipe', () => {
  it('create an instance', () => {
    const pipe = new BlankNamePipe();
    expect(pipe).toBeTruthy();
  });
});
